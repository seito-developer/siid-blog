{
  "mcpServers": {
    "microcms": {
      "command": "npx",
      "args": ["-y", "microcms-mcp-server@latest"],
      "env": {
        "MICROCMS_SERVICE_ID": "siid-web",
        "MICROCMS_API_KEY": "${MICROCMS_API_KEY}"
      }
    }
  }
}
